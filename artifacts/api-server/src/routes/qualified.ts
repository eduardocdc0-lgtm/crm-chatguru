// Arquivo mantido vazio — os endpoints de backfill foram removidos.
// O critério de qualificação agora usa status = 'lead_qualificado' (pipeline do webhook).
import { Router } from "express";
const router = Router();
export default router;
